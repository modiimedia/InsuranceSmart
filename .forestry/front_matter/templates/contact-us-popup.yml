---
label: Contact Us Popup
hide_body: true
fields:
    - name: title
      type: text
      config:
          required: true
      label: Title
      default: SCHEDULE A FREE CONSULTATION
    - name: subtitle
      type: text
      config:
          required: false
      label: Subtitle
      default: Contact us today to get expert advice concerning your insurance goals
    - name: style
      type: select
      default: light
      config:
          required: true
          options:
              - light
              - dark
          source:
              type: simple
              section:
              file:
              path:
      label: Style
    - name: background_image
      type: file
      config:
          maxSize: '15'
      label: Background Image
